# Volta

Панель опционов: futures + options (Bybit/unicex) и overlay Polymarket.

Полная документация: [docs/README.md](docs/README.md)

```text
cd app-options
cp .env.example .env
docker compose --env-file .env up --build -d
```

Панель: http://localhost:8081/login
